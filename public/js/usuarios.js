/**
 * Created by Carlos on 25/08/2017.
 */

function Login(){
    var done=0;
    var usuario=document.login.usuario.value;
    var password=document.login.password.value;
    if (usuario=="cognitivelegal" && password=="ibm") {
        window.location="inicio.html";
    }
    if (usuario=="alejandro" && password=="soa") {
        window.location="inicio.html";
    }
    if (usuario=="diego" && password=="soa") {
        window.location="inicio.html";
    }
    if (usuario=="jorge" && password=="soa") {
        window.location="inicio.html";
    }
    if (usuario=="santos" && password=="soa") {
        window.location="inicio.html";
    }
}